# Optional BGE cross-encoder reranker stack (disabled by default).
# Only required if you enable reranking: set reranker_enabled: True in config.py.
# Adds ~2.5 GB of PyTorch deps plus a ~1.3 GB model download from HuggingFace.
torch==2.1.0
transformers==4.34.1
sentence-transformers>=3.0.0
